<Window x:Class="MeetingScheduler.View.EditEvent"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MeetingScheduler.View"
        mc:Ignorable="d"
        Title="EditEvent" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBox Grid.Row="0" Height="200" Text="{Binding appointment.EventType , UpdateSourceTrigger=PropertyChanged}" FontSize="16"></TextBox>
            <TextBox Grid.Row="1" Height="200" Text="{Binding Subject, UpdateSourceTrigger=PropertyChanged}" FontSize="16" Margin="0,5,0,10"></TextBox>
            <TextBox Grid.Row="2" Height="200" Text="{Binding appointment.Subject, UpdateSourceTrigger=PropertyChanged}" FontSize="16" Margin="0,5,0,10"></TextBox>
        </StackPanel>
        
    </Grid>
</Window>
